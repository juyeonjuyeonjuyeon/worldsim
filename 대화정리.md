# WorldSim 프로젝트 대화 정리
날짜: 2026-06-16

---

## 목표

상상한 세계 공간을 물리 엔진 기반으로 시뮬레이션하고, 시간의 흐름에 따른 변화를 관찰하는 앰비언트 영상을 만든다.
AI가 생성한 그럴싸한 영상이 아닌, 실제 물리 법칙에 기반한 시뮬레이션.

---

## 만들고 싶은 세계관

1. **숲**
   - 계절과 날씨별 시간 변화
   - 상상의 공간 또는 실제 나라별 기후/지형 특성 모방 (열대우림, 침엽수림, 단풍림 등)

2. **우주/행성**
   - 화성에서 노을 보기 (실제 화성 대기 물리 기반)
   - 특정 행성 또는 가상의 행성 (중력, 대기 조성, 지표 물질 설정)
   - 참고: 화성 노을은 낮엔 붉고 해질 때 파랗게 변함 (지구와 반대)

3. **조선시대 한옥 24시간 관찰**
   - 처마 밑으로 스며드는 빛, 마당의 그림자 변화 등

---

## 도구 선택

### 검토한 도구들
- **Genesis (Genesis-Embodied-AI)**: 로봇 시뮬레이션 특화. 대기 산란, 구름, 자연환경 미지원 → 부적합
- **Unreal Engine 5**: 강력하지만 오브젝트 직접 배치 필요, GUI 중심 → 진입장벽 높음
- **Blender + Python**: 물리 기반 렌더러 + 대기 모델 + Python 완전 제어 → 선택

### Blender를 선택한 이유
- Python(bpy)으로 모든 것을 코드로 제어 가능
- 물리 기반 대기 산란 모델 내장 (Blender 5.1 기준: MULTIPLE_SCATTERING)
- Cycles 레이트레이싱 렌더러 (빛 산란, 그림자, 대기 효과 자동 계산)
- 완전 무료 및 오픈소스
- 헤드리스 실행 가능 (GUI 없이 스크립트만으로 렌더링)

---

## 구현 방식

### 전체 흐름
```
파이썬 스크립트 작성
    ↓
Blender 헤드리스 실행
    ↓
장면 자동 생성 (지형, 나무, 에셋 절차적 배치)
    ↓
물리 기반 시뮬레이션 (대기, 조명, 날씨)
    ↓
렌더링 → 영상 출력
```

### Blender 실행 명령어
```powershell
& "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --background --python "스크립트경로.py"
```

### 구현 가능한 요소
| 요소 | 방법 |
|------|------|
| 대기 산란 | MULTIPLE_SCATTERING (Blender 5.1 물리 모델) |
| 구름 | 볼류메트릭 + 노이즈 텍스처 |
| 안개 | 볼류메트릭 스캐터링 |
| 숲 생성 | 절차적 생성 (규칙 정의 → 자동 배치) |
| 24시간 사이클 | 태양 각도 애니메이션 (프레임 = 시간) |
| 빛 산란 | Cycles 레이트레이싱 (자동) |
| 계절 변화 | 나뭇잎 색상/밀도 애니메이션 |
| 날씨 | 비, 눈, 안개, 구름 파티클 시스템 |

### 시간 표현 방식
- Blender 프레임 = 시간 단위
- 예: 프레임 1 = 0시, 프레임 240 = 24시간
- 태양 각도(sun_elevation)를 시간 함수로 애니메이션

---

## 환경

| 항목 | 내용 |
|------|------|
| OS | Windows 11 Home |
| Python | 3.14.6 |
| Blender | 5.1.2 |
| GPU | NVIDIA GeForce RTX 5060 Laptop GPU |
| VRAM | 8GB |
| GPU 드라이버 | 596.08 |
| 디스크 여유 | 716GB |
| 추가 비용 | 없음 (전부 무료) |

---

## Blender 5.1 주의사항 (기존 튜토리얼과 다른 점)

| 항목 | 기존 (4.x 이하) | Blender 5.1 |
|------|----------------|-------------|
| 하늘 모델 | `'NISHITA'` | `'MULTIPLE_SCATTERING'` |
| 먼지 밀도 | `dust_density` | `aerosol_density` |
| use_nodes | 정상 | DeprecationWarning (아직 작동은 함) |
| 가용 sky_type | NISHITA 등 | SINGLE_SCATTERING, MULTIPLE_SCATTERING, PREETHAM, HOSEK_WILKIE |

---

## 테스트 결과 (2026-06-16)

### 첫 번째 테스트 렌더링
- **장면**: 평지 지형 + 나무 30그루 + 일출 하늘 (오전 6시)
- **해상도**: 1920 x 1080
- **샘플**: 128 + 디노이징
- **렌더링 시간**: 약 16초 (RTX 5060 GPU)
- **출력 파일**: `output\test_sunrise.png`
- **결과**: 성공

---

## 프로젝트 파일 구조

```
WorldSim/
├── 대화정리.md            ← 이 문서
├── test_render.py         ← 테스트 렌더 스크립트 (완료)
└── output/
    └── test_sunrise.png   ← 첫 번째 테스트 결과물
```

---

## 진행 이력

### 2차 작업 (2026-06-16)
- Blender GUI에서 인터랙티브 뷰 구성
- Blender 5.1에서 Sapling 애드온 제거 확인 → 절차적 생성으로 대체
- 나무 개선: 5단 테이퍼 기둥 + 가지 4~6개 + 잎 클러스터 10~16개
- Git 설치 및 GitHub 리포지토리 연결: github.com/juyeonjuyeonjuyeon/worldsim
- 최종 목표 확정: Genesis(물리) + Blender(렌더링) 파이프라인

### 3차 작업 (2026-06-16)
- Genesis 설치 완료 (genesis-world 1.1.2)
- Python 3.14 미지원 → Python 3.11 가상환경(genesis_env) 별도 생성
- PyTorch 2.11.0 + CUDA 12.8 설치 (RTX 5060 GPU 인식 확인)
- SPH 유체 시뮬레이션 10스텝 테스트 성공

## Genesis 환경 정보

| 항목 | 내용 |
|------|------|
| Genesis 버전 | 1.1.2 |
| 가상환경 | `WorldSim/genesis_env/` (Python 3.11) |
| 실행 명령 | `genesis_env\Scripts\python.exe script.py` |
| PyTorch | 2.11.0+cu128 |
| 백엔드 | gs.cuda (RTX 5060, 7.96GB) |
| 인코딩 주의 | `PYTHONIOENCODING=utf-8` 필요 |
| 유체 sampler | 'random', 'pbs', 'regular' (pcd 아님) |

### 4차 작업 (2026-06-16)
- Genesis 비 시뮬레이션 성공: 36,000 파티클 × 120프레임, 최대 97 FPS
- Genesis → Blender 파이프라인 완성 (npy → Shape Key 애니메이션)
- `rain_particles.npy` 저장 및 Blender 렌더링 완료
- 결과물: `output/rain_render.png`, `rain_world.blend`

## 파이프라인 구조 (완성)

```
Genesis (물리 계산)          Blender (렌더링)
────────────────────         ──────────────────
SPH 유체 시뮬레이션    →    rain_particles.npy 로드
36,000 파티클               Shape Key 애니메이션
120프레임 저장              MULTIPLE_SCATTERING 하늘
97 FPS (RTX 5060)           Cycles 렌더링 출력
```

### 5차 작업 (2026-06-16)
- Genesis 비 파티클 + Blender 숲 씬 통합 시도
- 문제: 버텍스 인스턴싱 방식 → Rendered 모드에서 까만 점만 표시
- 문제: Blender 파티클 시스템 → 백그라운드 렌더에서 파티클이 모두 DEAD 상태
  - 원인: 초기속도 + 중력으로 빗방울이 땅 아래로 너무 빠르게 떨어짐 (26프레임 만에 통과)
  - 진단: `emitter.evaluated_get(dg).particle_systems[0].particles` → alive_state='DEAD'

### 6차 작업 (2026-06-17)
- **비 렌더링 방식 전환: Curve 오브젝트 기반**
  - 3500개 Curve Spline (각 2포인트 = 빗줄기 하나)
  - `curve_data.bevel_depth = 0.005` → 5mm 두께 원통형 빗줄기
  - Z: 0.8~13m 사이 랜덤 분포 (장면 전체 높이)
  - 약간의 X/Y 기울기 → 바람에 흔들리는 비 표현
  - 재질: Transmission 0.70, IOR 1.333 (물 굴절률)
- 결과물: `output/rainy_forest.png`, `rainy_forest.blend`
- **비가 명확히 보이는 숲 씬 렌더링 성공**

### 8차 작업 (2026-06-17)
- Genesis SPH 연속 강우 구현 시도 및 문제 해결:
  - 시도 1: 큰 박스(4×4×10m) → 파티클 2천만개 폭발, 0.28FPS → 실패
  - 시도 2: 배치 반복 (`gs.init()` 반복호출) → "already initialized" 오류 → 실패
  - 해결: 한 씬에 얇은 슬랩 3개 (z=2, 5, 8m) → 108,000파티클, 137FPS → 성공
- Blender 렌더링 파이프라인 디버깅:
  - Geometry Nodes 인스턴싱 → 백그라운드에서 작동 안 함
  - Shape Key + 투명 재질(72%) → 배경에 묻혀 안 보임
  - 진단: 빨간 발광 재질로 테스트 → Curve 자체는 정상 렌더됨
  - 해결: 정적 Curve (Genesis 프레임80 위치) + 약한 발광(0.4) + 12mm 두께
- **최종 결과: Genesis SPH 물리 위치 기반 비 렌더링 성공**

### 9차 작업 (2026-06-17)
- Shape Key 애니메이션 방식 포기 → `frame_change_post` 핸들러 방식으로 전환
  - 문제: Shape Key 블렌딩이 렌더에서 비 위치를 잘못 계산 (원인: 블렌딩 수식 vs depsgraph 캐시)
  - 진단: static Curve(프레임0 위치 고정)는 비 보임 → Shape Key 애니메이션만 안 보임
  - 해결: `bpy.app.handlers.frame_change_post` 핸들러로 매 프레임 Curve 좌표 직접 기록
  - 핵심: `rain_obj.data.update_tag()` 호출 필수 → depsgraph에 변경 알림
- **결과: WS_forest_rain_v002.png — Genesis SPH 물리 궤적 기반 비 렌더링 완전 성공**
  - 비 줄기가 숲 씬 전체에 분포하여 선명하게 보임
  - Genesis 프레임 79 위치 (0.237초 낙하 물리값) 사용

## 기술 노트: Shape Key 실패 원인 분석

Shape Key 방식이 렌더에서 비가 안 보인 이유:
- Blender `bpy.ops.render.render()` 내부에서 depsgraph가 재평가됨
- Shape Key 애니메이션 값이 `frame_change_post` 이후에도 depsgraph 캐시에서 계산됨
- Python으로 Shape Key 값을 직접 설정해도 렌더 시점에 override될 수 있음
- `frame_change_post` 핸들러 + `update_tag()` 조합이 확실한 해결책

## 파일 네이밍 컨벤션 (VFX/애니메이션 스튜디오 방식)

ILM, Pixar 등 VFX 스튜디오 관행을 따름.

| 파일 종류 | 패턴 | 예시 |
|-----------|------|------|
| 씬 파일 (.blend) | `WS_scene_desc_v###.blend` | `WS_forest_rain_v001.blend` |
| 렌더 출력 (.png) | `WS_scene_desc_v###.png` | `WS_forest_rain_v001.png` |
| 스크립트 (.py) | `scene_desc.py` (버전 없음) | `forest_rain.py` |
| 물리 시뮬 데이터 | `WS_scene_desc_sim/` | `WS_genesis_rain_sim/` |

- `WS` = WorldSim 프로젝트 코드
- `v###` = 3자리 버전 (v001, v002 ...)
- 스크립트는 버전 없음 → git이 이력 관리
- 날짜는 파일명 제외 → git 커밋 타임스탬프로 추적

### 씬 코드 목록
| 씬 코드 | 설명 |
|---------|------|
| `forest_day` | 숲 낮 (24시간 사이클) |
| `forest_rain` | 비 내리는 숲 |
| `mars_sunset` | 화성 노을 (예정) |
| `hanok_dawn` | 한옥 새벽 (예정) |

---

## 기술 노트: 비 렌더링 방법 비교

| 방법 | 결과 | 문제 |
|------|------|------|
| Vertex Instancing | 까만 점만 표시 | Cycles에서 인스턴스 렌더 실패 |
| Particle System | 비 안 보임 | 백그라운드에서 파티클 DEAD (중력 > 수명) |
| Geometry Nodes | 비 안 보임 | 백그라운드 렌더에서 작동 안 함 |
| Shape Key 애니메이션 | 비 안 보임 | depsgraph 캐시가 Python 변경 무시 |
| **Curve + 프레임 핸들러** | **비 선명하게 보임** | **없음 → 채택** |

### 채택 방식 상세
- `bpy.app.handlers.frame_change_post.append(fn)`: 매 프레임 변경 시 Genesis 좌표를 Curve에 직접 기록
- `rain_obj.data.update_tag()`: depsgraph에 변경 알림 (렌더 반영 필수)
- Curve `bevel_depth=0.012` (12mm) + Emission 0.4: 배경 무관하게 가시성 보장

### 7차 작업 (2026-06-17)
- 파일 네이밍 컨벤션 VFX 스튜디오 방식으로 전환 (WS_scene_desc_v###)
- Genesis 시뮬 적용 현황 점검:
  - 현재 forest_rain.py는 Genesis 데이터 미사용 (Blender Curve 오브젝트로 대체)
  - Genesis 시뮬은 작동하나 연속 방출(continuous emission)이 구현 안 됨
- **목표 재확인**: "그럴싸해 보이는 게 아니라 실제 세계 구현"
  - 현재 Curve 비는 완전한 가짜 (random 함수로 위치 결정)
  - Genesis SPH 물리 기반 연속 강우 + 지면 충돌 + 물웅덩이가 목표
- **결정: C 방향 (Genesis 연속 방출 재작성 + Alembic 파이프라인)**

### 9차 작업 (2026-06-17)
- Shape Key 애니메이션 방식 포기 → `frame_change_post` 핸들러 방식으로 전환
  - 문제: Shape Key 블렌딩이 렌더에서 비 위치를 잘못 계산 (depsgraph 캐시 무시)
  - 해결: `bpy.app.handlers.frame_change_post` 핸들러로 매 프레임 Curve 좌표 직접 기록
  - 핵심: `rain_obj.data.update_tag()` 호출 필수
- **WS_forest_rain_v002.png**: Genesis SPH 물리 궤적 기반 비 렌더링 완전 성공

### 10차 작업 (2026-06-17)
- 비 품질 개선 (v003):
  - 파티클 수: 3500 → 5000
  - 빗줄기 두께: 12mm → 10mm
  - 빗줄기 길이: 0.35m → 0.40m
  - 풍각도 추가: `wind_tilt = 0.12` (수평 기울기 12%)
- 지면 스플래시 링 추가:
  - z < 0.35m 파티클을 다이아몬드형 Curve 링으로 표시
  - 프레임 핸들러로 매 프레임 업데이트
  - 최대 300개 동시 스플래시
- 웅덩이(Puddle) 추가:
  - 12개 원형 납작 mesh, 반사성 재질 (roughness 0.05)
  - 지면에 고인 빗물 표현
- 200프레임 PNG 시퀀스 렌더링 중:
  - 출력: `output/WS_forest_rain_v003/WS_forest_rain_v003_####.png`
  - 프레임당 ~12초, 총 ~40분
  - Blender VSE로 mp4 합치기 예정 (`make_video.py`)
- **WS_forest_rain_v003_preview.png**: 개선된 씬 프리뷰 확인

## 다음 단계 후보

- [x] 비 파티클을 숲 씬과 합치기 (forest + rain) ← 완료
- [x] 실제 빗방울 형태 (Curve 기반 빗줄기) ← 완료
- [x] Genesis SPH → frame_change_post 핸들러 애니메이션 ← 완료
- [x] 지면 물 튀김(splash) 효과 ← 완료 (다이아몬드 링)
- [x] 200프레임 PNG 시퀀스 렌더링 ← 진행 중
- [x] PNG → mp4 합치기 (ffmpeg, winget Gyan.FFmpeg 설치) ← 완료
- [x] Blender VSE → ffmpeg 방식으로 make_video.py 교체
- [x] v003.mp4 (3.6MB, 24fps, 8.3초) ← 완료
- [x] v004 200프레임 렌더 + v004.mp4 (3.7MB) ← 완료 (연속 강우 루프)

### 11차 작업 (2026-06-17) – 사용자 피드백 기반 v005 수정
v004 시청 후 사용자가 4가지 문제 지적:
1. "비가 흰색 원기둥이어서 비같지 않음" → bevel 10mm→4mm, emission 0.5→0.15, transmission 0.55→0.85
2. "물웅덩이가 안 보임" → 진단 결과 재질은 정상이었으나 반지름(0.4~1.4m)이 카메라에서
   너무 작고 멀어 안티에일리어싱에 묻힘. 반지름 1.4~3.0m로 확대, 개수 12→8개로 줄여 해결
3. "비 떨어지는 애니메이션이 이상함" → v004의 "오프셋 반반" 연속강우 트릭이 100프레임 지점에서
   파티클 절반이 갑자기 점프하는 부작용 있었음. 단순 순차 재생으로 복귀 (연속루프는 추후 재검토)
4. "객체 테두리 같은 게 보임" → 스플래시를 다이아몬드 4점 Curve(채워지지 않은 윤곽선)로 만들어서
   와이어프레임처럼 보임. 8각 N-gon 채움 메쉬로 교체

**디버깅 노트**: 웅덩이가 안 보이는 문제를 진단할 때 재질(색상/투명도/반사)을 여러 차례 의심하고
바꿔봤으나 변화 없었음 → 큰 원판 하나로 단독 테스트해서 재질은 정상임을 확인 →
실제 원인은 크기/거리였음. 작은 오브젝트는 재질이 맞아도 화면에서 안티에일리어싱+디노이징에
묻혀 안 보일 수 있다는 교훈.

v005 200프레임 렌더링 완료 → WS_forest_rain_v005.mp4 (2.3MB)

v005 검증 중 새 문제 발견: 시간이 지날수록 비가 옅어짐 (1회성 낙하라
땅에 닿은 파티클은 그걸로 끝). 사용자에게 해결 방향 질문 → "실제 자연세계에
걸맞게" 답변.

### 12차 작업 (2026-06-17) – v006: 개별 위상(per-particle phase) 연속 강우
물리적 근거: 중력 낙하는 시간 이동에 불변 → 파티클마다 "언제 떨어지기
시작했는지"를 다르게 주는 것은 실제로 다른 순간에 낙하를 시작한 빗방울들이
동시에 존재하는 것과 물리적으로 동등함. 실제 비도 모든 방울이 동시에
생성되지 않고 각자 다른 시점에 시작됨.

```python
np.random.seed(123)
_phase = np.random.randint(0, n_frames, size=n_rain)   # 파티클별 랜덤 위상
# 매 프레임:
idx = (f + _phase) % n_frames        # 파티클마다 다른 시간 이동
pts = rain[idx, np.arange(n_rain), :]  # 팬시 인덱싱
```

v004의 "반반 오프셋"과 차이: v004는 2그룹으로 거칠게 나눠 100프레임 지점에서
동기화된 점프가 보였음. v006은 5000개 전부 독립적 위상이라 점프가 보이지
않고 무한 루프도 가능.

검증: 프레임 1/60/150/200 모두 비 밀도 일정함 확인 (시간에 따른 옅어짐 해결).
v006 200프레임 렌더링 완료 → WS_forest_rain_v006.mp4 (2.3MB)

### 13차 작업 (2026-06-17) – 비자연스러운 지점 전수조사 (수정 없이 발견만)
사용자가 v005에서 "비 오기 전부터 웅덩이가 있다"는 시간적 불일치를 발견 →
스크립트 전체와 Genesis npy 데이터를 직접 열어 비슷한 문제들을 조사.

발견 목록:
1. 웅덩이가 프레임 1부터 정적 배치 (사용자 발견)
2. 지면 재질("WetGround")도 비 내리기 전부터 이미 젖은 색
3. **XY만 25배 확대, Z는 그대로 (이방성 스케일링)** – 가장 근본적 문제.
   Genesis는 1.2m×1.2m 박스에서만 계산했는데 화면엔 30m×30m로 펴서 보여줌.
   입자간 거리/군집 패턴이 실제 계산 스케일과 무관하게 왜곡됨.
4. **SPH 초기화 불안정성 – npy 데이터 직접 분석으로 검증**:
   low/mid/high 각 층 입자의 ~0.5%(층당 195~197개)가 의도된 박스 범위를
   벗어남 (예: low층인데 frame0 z=4.5m). `z0<11.0` 필터는 극단값만 제거.
5. 스플래시는 사실상 "low 배치 출신" 입자만 발생 (mid/high는 200프레임 내
   자유낙하로 z<0.3m에 도달 못함)
6. v006도 입자별로 루프당 1회 "순간이동" 잔존 (동기화만 없앤 것)
7. 8.3초 후 정확히 반복되는 유한 루프
8. 스플래시 최대 300개 제한 → 초과시 인덱스 순서상 뒤쪽 입자는 스플래시 없음
9. 스플래시 크기 항상 균일(0.55m), 충돌 속도(낙하 높이)와 무관
10. 웅덩이(z=0.06)·스플래시(z=0.07~0.08) 고정 높이 vs 지형 Displace(±0.8m) 굴곡 불일치
11. wind_tilt=0.12가 비물리적 – Genesis 시뮬엔 바람 없음(중력만), Blender에서 사후 적용
12. 웅덩이-빗방울 상호작용 없음 (정적/동적 완전 분리)

사용자 지시: "고치거나 영상으로 만들지는 말고 찾기만 해" → 수정 안 하고 목록만 보고.

### 16차 작업 (2026-06-20) – Genesis 차단 해결 + 근본적 재시뮬레이션 (v009)

**Smart App Control 진단**: `Get-MpComputerStatus`로 확인한 결과
`SmartAppControlState: On`. 이 기능은 한번 켜면 Windows 재설치 전까지
끌 수 없는 단방향 기능. mujoco DLL이 서명되지 않아 차단됨.

**우회 방법**: Genesis는 SPH만 써도 내부적으로 RigidEntity/MJCF 모듈을
import하며 mujoco를 함께 불러옴. 실제로는 안 쓰는 기능이므로, `mujoco`를
가짜 모듈로 `sys.modules`에 미리 등록해 import만 통과시킴 (실제 DLL은
로드 안 됨, Windows 보안 정책 자체는 그대로 유지). 사용자 명시적 승인
후 적용. 첫 시도에서 `RuntimeError`로 스텁을 막았다가 `hasattr()`이
`AttributeError`만 인식한다는 걸 깨닫고 수정 → 정상 작동.

**더 큰 발견 - 기존 데이터 자체가 처음부터 불안정했음**: 박스를 키우는
작업 중 모든 시도(particle_size 조정, dt 축소, stiffness 조정)가 계속
폭주해서 원인을 거슬러 올라가다, **기존에 "검증됐다"고 믿었던
rain_particles.npy를 직접 분석**한 결과:
- 프레임 0: 폭주 0%
- 프레임 30: ~2.7%
- 프레임 100: ~18%
- 프레임 199(마지막): **37%**

폭주한 입자는 좌표가 ±100까지 튀었지만, XY를 25배 확대하면서 화면 밖
(±2500m)으로 날아가 v002~v008 어떤 렌더에서도 안 보였던 것.

**근본 원인**: `sampler='random'`이 초기 입자를 불균일/겹치게 배치해
SPH 압력이 첫 스텝부터 폭주(CFL 조건 위반과는 별개의 문제 - dt/stiffness
조정만으론 해결 안 됐음). `sampler='regular'`(균일 격자)로 교체 +
stiffness 50000→5000 조합으로 450프레임 전체 검증, 폭주 0건 확인.

**genesis_rain.py 전면 재작성**:
- 박스 크기: 1.2m → 30m (Blender 표시 영역과 1:1, XY 스케일 왜곡 완전 해결)
- particle_size: 0.17 (입자 수를 레이어당 ~31000개로 유지)
- 시뮬레이션 길이: 200 → 450프레임 (8m 높이 배치가 자유낙하로 땅에
  완전히 닿는 데 필요한 ~426스텝 확보 → mid/high도 스플래시 발생 가능)
- 결과: (450, 92928, 3) 배열, 479MB, 450프레임 내내 폭주 0건

**forest_rain.py 수정**:
- XY ×25 스케일링 코드 제거 (더 이상 불필요)
- 레이어별 이상치 필터링 제거 (regular 샘플러라 이상치 없음)

**새로 발견된 버그 - "흰 기둥" 아티팩트**: 위 수정 후 렌더에 거대한 흰
막대가 나타남. 스플래시/웅덩이를 하나씩 숨겨가며 격리한 결과 WS_Rain
(빗줄기) 자체가 원인으로 확인. tail-pts 거리 체크는 정규화 때문에 항상
0.4로 고정되는 무의미한 검증이었음 (재확인 필요). 위상(phase)을 끄고
동기재생하면 막대가 사라짐을 확인 → **원인: `regular` 샘플러는 3개
레이어가 동일한 XY 격자를 공유하는데, 입자별 완전 무작위 위상을 주면
같은 XY에서 유래한 다른 레이어 입자들이 서로 다른 무작위 시점(=다른
높이)에 놓이게 되고, 얕은 카메라 각도에서 그 XY 열 전체가 빗줄기로
"채워져" 막대처럼 보임**. 고주파 해시(XY*137+XY*251) 시도는 인접
격자점끼리도 위상이 급변해 동일하게 재발 → 저주파 그라디언트
(`(x0+15+y0+15)/60*n_frames`)로 교체해 해결 (인접 입자는 항상 비슷한
위상을 가져 높이 불연속 없음).

**부가 발견(이전 대화 중 사용자 질문)**: v008에서 비가 옆으로 날아다니듯
보인 것은 바람이 아니라 XY 25배 스케일 왜곡의 부작용이었음 - 실제 SPH의
작은 횡방향 흔들림(압력/입자간 상호작용)이 25배로 뻥튀기되어 속도
벡터의 가로 성분이 인위적으로 거대해진 것. v009에서 스케일 왜곡을
없애면서 자동으로 해결됨 (빗줄기가 정상적으로 수직 낙하).

검증: 프레임 1/200/449 모두 막대 없이 정상 렌더 확인.
v009 450프레임 렌더링 완료 → WS_forest_rain_v009.mp4 (10.5MB, 18.75초)
실제 렌더 프레임 1/450 재확인: 막대 없음, 빗줄기 수직 낙하 정상.

## 12개 발견사항 최종 상태 (2026-06-20 갱신)

| # | 문제 | 상태 |
|---|------|------|
| 1 | 웅덩이가 처음부터 존재 | ✅ v007/v008 완료 |
| 2 | 지면이 처음부터 젖은 색 | ✅ v008 완료 |
| 3 | XY만 25배 스케일 왜곡 | ✅ **v009 완료** (Genesis 박스 30m로 재시뮬레이션) |
| 4 | SPH 초기화 이상치 | ✅ v009 완료 (근본 수정: regular 샘플러) |
| 5 | 스플래시가 low배치만 발생 | ✅ **v009 완료** (450프레임으로 mid/high도 착지) |
| 6 | 개별 입자 순간이동 | 🔶 완화 (위상 적용 방식은 유지, 근본은 연속방출 시뮬레이션 필요) |
| 7 | 8.3초 유한 루프 반복 | 🔶 18.75초로 연장됐으나 여전히 유한 루프 |
| 8 | 스플래시 300개 인덱스 편향 | ✅ v007 완료 |
| 9 | 스플래시 크기 균일 | ✅ v007 완료 |
| 10 | 웅덩이/스플래시 z고정 vs 지형굴곡 | ✅ v007 완료 |
| 11 | wind_tilt 비물리적 | ✅ v007 완료 |
| 12 | 웅덩이-빗물 상호작용 없음 | ✅ v008 완료 |

**부가 발견**: v009 작업 중 "비가 옆으로 날아다니는" 것처럼 보인 v008의
현상도 #3(스케일 왜곡)의 직접적 부작용이었음이 확인됨 - 별도 항목은
아니었으나 같은 근본 원인으로 해결됨.

**부가 버그(이번에 발견 및 해결)**: regular 샘플러 도입으로 새로 생긴
"흰 기둥" 아티팩트 - 위상을 XY의 저주파 그라디언트 함수로 변경해 해결.

### 17차 작업 (2026-06-20) – v010: 사용자 피드백 2건 + 검수 요청
v009 시청 후 사용자 피드백: "비 떨어지는 거랑 웅덩이가 따로 노는 것
같다", "비가 하나씩 떨어지는게 아니라 왜 동시에 같은 속도로 떨어져?"

원인 분석:
- 동시 낙하 문제: v009에서 "흰 기둥" 버그를 고치려 위상을 완전히
  매끄러운 함수로 바꿨더니 인접 빗방울들이 거의 같은 위상을 가져
  "파도"처럼 보임
- 웅덩이 분리감: 웅덩이 위치/성장은 원본(위상 미적용) 데이터로 계산,
  화면에 보이는 비는 위상 적용된 다른 데이터 — 서로 다른 데이터 기반

수정: 완만한 그라디언트 + 작은 사인파(±25프레임) 추가로 개별성 보강,
파문 확대 배율 1.7→2.5로 인과관계 강조. v010 450프레임 렌더 완료
(WS_forest_rain_v010.mp4, 11.1MB).

**검수 요청에 대한 핵심 발견**: 사용자가 "실제 자연환경과 다른점이나
오류 검수"를 요청 → npy 데이터를 직접 분석한 결과 **같은 레이어 입자들의
Z 표준편차가 모든 프레임에서 정확히 0.000000** — SPH 입자간 압력
상호작용이 전혀 없고 단순 동일 중력 자유낙하와 다를 바 없음. regular
샘플러(완벽 대칭 격자)는 안정성을 위해 모든 유체다움을 희생시켰던 것.

### 18차 작업 (2026-06-20) – v011: SPH를 와이드샷에서 완전히 제거
사용자 지시: "선택지 2(작은 영역 클로즈업)로 가고, 비 떨어지는 건 클로즈업
때만 상세히 보여주고 나머지는 실제 자연세계와 동일하게"

**핵심 통찰**: SPH 안정성 vs 상호작용 트레이드오프를 더 파고들기 전에,
"SPH로 푸는 문제 자체가 틀렸다"는 걸 깨달음. 실제 빗방울은 낙하 중
서로 유체역학적으로 상호작용하지 않음(독립된 투사체, 중력+공기저항만
받음). SPH가 진짜 필요한 건 충돌/스플래시 순간뿐.

**시도한 것 (5가지 지터+stiffness 조합, 결론: 중간지대 없음)**:
| 지터 | stiffness | Z 상호작용 | 폭주 |
|---|---|---|---|
| 0.03 | 5000 | ~0 | 0 |
| 0.03 | 50000 | ~0 | 0 |
| 0.05 | 50000 | ~0 | 0 |
| 0.07 | 50000 | ~0 | 2건 |

`set_particles_pos`로 격자에 직접 지터 주입(Genesis에 옵션 없음 확인,
Agent로 소스 조사: particle.py의 `_box_to_particles`가 'regular'는
순수 linspace 격자, 'random'은 독립 균등난수, 'pbs'는 Windows 미지원).

**새 설계 (genesis_rain.py 완전 우회)**:
- 마샬-팔머(Marshall-Palmer) 빗방울 크기분포: N(D)=N0·exp(-ΛD)
- 건-킨저(Gunn-Kinzer, Atlas et al. 1973) 종단속도 경험식:
  v_t = 9.65 - 10.3·exp(-0.6D)
- 2차 공기저항 해석해: v(t)=v_t·tanh(gt/v_t), 낙하거리=(v_t²/g)ln(cosh(gt/v_t))
  → numpy로 정확한 실제 물리값 계산, GPU/SPH 불필요
- 각 빗방울이 독립된 무작위 시각(drop_phase)에 낙하 시작 — 위상 트릭이
  아니라 실제 기상학적 사실(빗방울들은 동시에 응결되지 않음)
- 큰 방울은 빠르게, 작은 방울은 느리게 떨어지는 다양성이 자연 발생
  (더 이상 시각 트릭으로 흉내내는 게 아님)

**부수 발견+수정**: 순수 균등난수로 XY를 뽑자 v009의 "흰 기둥"과 동일한
원인의 버그 재발 — 직접 확인 결과 반경 0.4m 안 평균 2.8개여야 할 입자가
실제로 11개 뭉친 통계적 클러스터 발견. 지터드 그리드(각 셀에 정확히
1개, 셀 내부에서만 무작위)로 교체해 최소 간격 보장, 군집 원천 차단
(재검증: 최대 6개로 정상 범위).

v011 렌더링 완료 → WS_forest_rain_v011.mp4 (4.5MB, 168프레임/7초 루프).
실제 렌더 프레임 1/100 확인: 막대 없음, 빗줄기 길이가 속도에 따라
제각각(개별 물리 다양성이 시각적으로 드러남), 웅덩이/스플래시 정상.

**남은 작업**: 클로즈업 스플래시 전용 소규모 SPH(작은 영역, 빗방울
크기 입자)는 아직 미구현 — 다음 단계.

### 19차 작업 (2026-06-21) – v012/v013: 그래픽 디테일 개선 (사용자 검수)

**v012 - 웅덩이 "판떼기" 문제**: 사용자가 "웅덩이가 평평한 색만 있는
도형이라 물처럼 안 보이고, 지형 파인 부분에 안 맞아 판떼기 같다" 지적.
원인: 평평한 원판을 단일 높이(중심점 기준)로 배치해서 굴곡진 지형에서
솟은 곳은 묻히고 꺼진 곳은 떴음. 수정: 지형 높이 그리드에서 실제
움푹한 부분(주변 하위 25%)을 찾아 격자 셀 단위로 그 윤곽대로 블롭
메쉬를 쌓음 (수면은 항상 평평, 외곽선만 지형 추적). 재질에 약한
잔물결 범프도 추가. WS_forest_rain_v012.mp4 (4.4MB) 완료.

**부가 질문(개념 정리)**: "Genesis 없이도 실제 물리 가능한가" /
"사운드도 계산 가능한가" 논의 — 닫힌 형태 해가 있으면(독립 투사체)
시뮬레이션 엔진 불필요, 다체 상호작용(충돌/스플래시)에만 필요하다는
원칙 확인. 사운드도 같은 원리(Minnaert 기포 공명 공식)로 가능하다는
방향 논의, 구현은 보류.

**v013 - 하늘 "어색함" 문제**: 사용자가 "하늘도 어색해" 지적. 픽셀
직접 확인 결과 화면 중간의 청회색 영역이 실제로는 나무+웅덩이 섞인
먼 지면이었고, 진짜 하늘은 화면 위쪽 2~3%뿐이었음(카메라 X=75°가
거의 수직 하향이라). 사용자 지시: "하늘과 땅이 같이 보이게, 비율은
알아서". 35mm 렌즈 수직시야각(~32°) 계산해 카메라 X=84°로 조정
(하늘:땅 약 1:2, 삼분할법), 위치도 살짝 높이고 후퇴. 지형도 800m
평면으로 지평선 확장(50m 끝에서 하늘로 칼같이 끊기는 문제). 볼류메트릭
구름 추가(텍스처 트릭 아니라 노이즈 밀도 기반 실제 광 산란) — 1차
시도(4m 두께)는 얕은 각도에서 단단한 천장처럼 보여 16m로 두껍게 +
밀도 낮춰 해결. v013 렌더링 완료 → WS_forest_rain_v013.mp4 (3.9MB,
164프레임). 하늘:땅 비율과 구름 질감 모두 의도대로 확인.

### 14차 작업 (2026-06-19) – v007: 발견한 문제들 수정 시도
사용자 지시: "그것들을 다 고쳐줘" → 12개 문제 전부 수정 시도.

**차단된 작업 (환경 문제, 수정 불가)**:
- Genesis 재시뮬레이션이 막힘: `mujoco._callbacks` DLL이 Windows
  "애플리케이션 제어 정책"(Smart App Control 또는 WDAC 추정)에 차단됨
- 내 코드 변경과 무관함을 확인: 옵션 없는 베이스라인 `gs.Scene()` 생성조차
  동일하게 실패 → 시스템 환경이 이전 세션 이후 바뀐 것으로 추정
- 영향: XY 스케일 왜곡(25x) 자체 해결(박스 키우기), mid/high 배치 스플래시
  추가(더 긴 시뮬레이션) 등 새 시뮬레이션이 필요한 항목은 보류
- **사용자 확인 필요**: Windows 보안 정책 설정에서 mujoco DLL 허용 여부

**수정 완료 (기존 npy 데이터만으로 가능했던 것들)**:
1. SPH 초기화 이상치 필터링 강화: 레이어별 기대 범위(±0.5m) 밖 입자 제거
   → 108000개 중 1176개(1.1%) 제외 확인
2. 웅덩이를 실제 강우 낙하 밀집도 기반으로 배치 (`np.histogram2d`로 2D 밀집
   지역 탐색, `random.seed(42)` 임의 배치 제거). 크기도 밀집도에 비례
3. 웅덩이/스플래시 위치를 지형 raycast 높이 그리드(48x48)로 사전계산해
   실제 굴곡진 지표면에 정확히 배치 (이전엔 고정 z값이라 뜨거나 묻힘)
4. wind_tilt(고정 12% 기울기, 비물리적) → 프레임간 실제 변위(Genesis 계산
   속도)로 빗줄기 방향 결정. 위상 wrap 경계(속도 깨지는 지점)는 평균
   낙하속도로 대체
5. 스플래시 300개 제한 초과시 인덱스 순서 편향 → `np.random.choice` 랜덤
   샘플링으로 수정
6. 스플래시 크기를 충돌 속도(수직 낙하속도) 기반으로 가변화 (0.3~1.3m)

검증: 프레임 1/60/100/150/200 모두 정상 렌더, 비 밀도 일정, 에러 없음
v007 200프레임 렌더링 완료 → WS_forest_rain_v007.mp4 (1.6MB)

### 15차 작업 (2026-06-19) – v008: 남은 비자연스러운 지점 마무리
사용자 지시: "차단으로 못 고치는 것 외에 다 고칠 것" → 13차에서 손 안 댄
나머지 3개(웅덩이 동적 성장, 지면 시간적 습윤, 웅덩이-빗물 상호작용) 수정.

1. **웅덩이 동적 성장**: 단위원(반지름1)으로 만들고 object.scale을 매 프레임
   조절. 프레임 1엔 거의 안 보이다가 비가 쌓이며 점차 자람.
   - 디버깅: 처음엔 "현재 자란 반지름" 안에 적중해야 성장하게 짜서
     반지름이 0이라 영원히 안 자라는 닭-달걀 버그 발생 → 성장 판정은
     웅덩이의 "최종 크기"(고정 집수 영역)로, 파문 표시만 "현재 크기" 기준으로 분리
   - 성장률 5→0.05에서 0.15로 상향 (150프레임 내 가시적 크기 도달 위해)
2. **지면 습윤 시간 변화**: 정점색(WetnessMask)에 공간적 낙하 밀집도 저장,
   셰이더의 Value 노드(GlobalWetnessRamp)를 프레임 핸들러가 매 프레임
   갱신해 dry_color↔wet_color Mix. 60프레임에 걸쳐 완전히 젖음.
   - 디버깅: 밀집도 마스크가 좁은 패치에만 몰려서(스케일 왜곡 #3과 연결된
     문제) 시간이 다 지나도 대부분 지역이 마른 색으로 남는 부작용 발견
     → MapRange로 최소 50% 베이스라인 부여 (밀도 낮은 곳도 절반은 젖게)
3. **웅덩이-빗물 상호작용**: 스플래시가 웅덩이의 "현재 자란 반지름" 안에
   떨어지면 1.7배 크게 그려 파문처럼 보이게 함

검증: puddle_wetness, ground_wetness_value 프레임별 출력으로 성장 곡선 확인.
실제 렌더 프레임 1 vs 200 비교 결과: 프레임 1엔 웅덩이 안 보이고, 프레임
200엔 나무 옆에 어두운 웅덩이 생성 확인 → 동적 성장 시각적으로 작동함.
v008 200프레임 렌더링 완료 → WS_forest_rain_v008.mp4 (1.9MB)

## 12개 발견사항 최종 상태 (2026-06-19 기준)

| # | 문제 | 상태 |
|---|------|------|
| 1 | 웅덩이가 처음부터 존재 | ✅ v007(위치)+v008(동적성장) 완료 |
| 2 | 지면이 처음부터 젖은 색 | ✅ v008 완료 (시간 램프) |
| 3 | XY만 25배 스케일 왜곡 | ❌ Genesis 차단으로 미해결 |
| 4 | SPH 초기화 이상치 | ✅ v007 완료 (1176개 제외) |
| 5 | 스플래시가 low배치만 발생 | ❌ Genesis 차단으로 미해결 |
| 6 | 개별 입자 순간이동 | ❌ Genesis 차단으로 미해결 |
| 7 | 8.3초 유한 루프 반복 | ❌ Genesis 차단으로 미해결 (6과 동일 원인) |
| 8 | 스플래시 300개 인덱스 편향 | ✅ v007 완료 (랜덤 샘플링) |
| 9 | 스플래시 크기 균일 | ✅ v007 완료 (충돌속도 기반) |
| 10 | 웅덩이/스플래시 z고정 vs 지형굴곡 | ✅ v007 완료 (raycast) |
| 11 | wind_tilt 비물리적 | ✅ v007 완료 (실제 속도 기반) |
| 12 | 웅덩이-빗물 상호작용 없음 | ✅ v008 완료 (파문 효과) |

**9개 완료, 3개 미해결(전부 Genesis 환경 차단).** mujoco DLL 차단 정책이
풀리면 #3/#5/#6/#7을 마무리할 수 있음.
- [ ] Genesis 시뮬 데이터 → Alembic 내보내기 (더 나은 파이프라인)
- [ ] 안개/구름 볼류메트릭 추가
- [ ] 화성 노을 씬 제작 (WS_mars_sunset)
- [ ] 조선 한옥 씬 제작 (WS_hanok_dawn)
- [ ] 계절 변화 애니메이션
- [ ] Genesis 연속 강우 (파티클 재생성 – 진정한 연속 강우)

---

*저장 위치: C:\Users\kkjjy\Documents\WorldSim\대화정리.md*
